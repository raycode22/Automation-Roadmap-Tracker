import React from 'react'
import ReactDOM from 'react-dom/client'
import BootcampApp from '../bootcamp-app'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <BootcampApp />
  </React.StrictMode>,
)
